<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>سلة المشتريات | الفرسان</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body style="padding: 20px;">
    <div class="container">
        <h1>🛒 سلة المشتريات</h1>
        <table style="width: 100%; background: white; border-radius: 8px;">
            <thead>
                <tr>
                    <th>المنتج</th>
                    <th>المقاس</th>
                    <th>السعر</th>
                </tr>
            </thead>
            <tbody>
                <% cart.forEach(item => { %>
                <tr>
                    <td><%= item.name %></td>
                    <td><%= item.size %></td>
                    <td><%= item.price %> ج.م</td>
                </tr>
                <% }) %>
            </tbody>
        </table>
        
        <div style="margin-top: 20px; text-align: left; padding: 20px; background: #eee;">
            <h3>الإجمالي: <%= total %> ج.م</h3>
            <p style="color: green;">(تم تطبيق خصم الموقع بنجاح)</p>
            <a href="/checkout-all" class="buy-btn" style="background: #27ae60;">تأكيد وطلب الأوردر</a>
        </div>
        <script>
function sendToWhatsApp() {
    // رقم موبايلك اللي حددته
    const myNumber = "201003644366"; 
    
    // جمع بيانات العميل من الفورم (الاسم، العنوان، المنتجات)
    const name = document.getElementById('customer-name').value;
    const address = "بني سويف - شارع عبد السلام عارف";
    const products = "قائمة المشتريات من سلة الفرسان..."; // بنجمعها من السلة
    
    const message = `طلب جديد من متجر الفرسان 🏁%0A` +
                    `👤 الاسم: ${name}%0A` +
                    `📍 العنوان: ${address}%0A` +
                    `📦 الطلب: ${products}%0A` +
                    `💰 الإجمالي بعد الخصم: ...`;

    // فتح واتساب فوراً
    window.open(`https://wa.me/${myNumber}?text=${message}`, '_blank');
}
</script>
<form action="/place-order" method="POST">
    <input type="text" name="name" placeholder="اسمك بالكامل" required>
    <input type="text" name="phone" placeholder="رقم الموبايل" required>
    <input type="text" name="address" value="بني سويف - شارع عبد السلام عارف" readonly>
    
    <button type="submit" class="save-btn" style="background: #27ae60;">
        تأكيد الطلب وإرسال واتساب ✅
    </button>
</form>
    </div>
</body>
</html>